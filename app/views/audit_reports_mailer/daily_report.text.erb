Daily Audit Report
==================

Report Period: <%= @from_date.localtime.strftime('%Y-%m-%d %H:%M %Z') %> to <%= @to_date.localtime.strftime('%Y-%m-%d %H:%M %Z') %>

Employees with Status Changes: <%= @employee_count %>

<% if @employee_count > 0 %>
This report shows employees who had status changes (appointments,
terminations, transfers, etc.) during the specified time period, along
with their current account statuses and any open account requests.

The complete report data has been attached as a CSV file for your review.

Summary
-------
Total employees with status changes: <%= @employee_count %>
Report generated: <%= Time.now.strftime('%Y-%m-%d %H:%M:%S %Z') %>
<% else %>
No employees had status changes during this time period.
<% end %>

<% if @report_url %>
View this report online:
<%= @report_url %>

<% end %>
---
This is an automated report from the NY Senate Audit Utils system.
